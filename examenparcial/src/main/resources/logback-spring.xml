<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Define el patrón de salida de los logs -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/error.log</file>
        <append>true</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Agrega un appender adicional para mostrar logs en la consola -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Define el nivel de logging para todos los logs -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

    <!-- Solo guarda errores en el archivo de log -->
    <logger name="com.examenparcial" level="ERROR" additivity="false">
        <appender-ref ref="FILE" />
    </logger>
</configuration>
